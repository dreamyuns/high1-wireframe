# High1 OTA — 통합 개발 스펙 (티켓·쿠폰 도메인)

> **목적**: 개발사가 실제 구현할 수 있도록 티켓(쿠폰) 어드민의 데이터 모델·화면 동작·비즈니스 로직·예외 케이스를 통합 정의한 개발 스펙.
> **범위**: 티켓·쿠폰(단품·오픈형) — 카테고리·쿠폰 스펙·티켓 상품·마진·주문·발급현황·현장연동 API. **패키지(RM+CPN)·투어·날짜지정형·판매유형(얼리/상시/프로모션) 제외**(별도 라인/이후 단계).
> **기준**: 티켓쿠폰연동-정책서 v0.11 · 어드민 프로토타입 v0.7.18 / 프런트 v0.8.9 · 화면기획서(admin_wireframe S11·S13~S17)
> **버전**: v1.3 · 2026-07-10
> **참조**: `티켓_쿠폰정책/티켓쿠폰연동-정책서-v0.11.md`(정책), `api연동/하이원소셜연동_인터페이스규격서_2018706_v1.0.docx`(현장 API), `admin_wireframe/s1[1,3-7]-*.html`(화면기획서), 자매 문서 `High1-OTA-통합개발스펙-숙소-v1.2.md`

> **⚙ 구현 현황(프로토타입 admin v0.7.18 / front v0.8.9)**
> - **어드민 구현 완료**: S17 **카테고리/컨텐츠 관리**(카드형·2뎁스 + 뎁스 콘텐츠[히어로·개요·상세·안내·정책·위치] 리치 에디터) · S13 쿠폰 스펙(할인대상내역·카테고리 칩·더미 CSV) · S14-A 상품 목록 · S14-B 상품 마스터(**2탭**: 기본정보+썸네일 / 날짜) · **S11 마진(숙소/티켓 탭 — 카테고리 마스터+쿠폰ID 오버라이드 편집 완료)**.
> - **프런트 구현 완료**: 검색바 도메인 토글(사용일 +7 기본) · 검색결과(숙소식 canonical 카드) · 카테고리 브릿지(숙소식 place-selector·hero·place-nav·장바구니형 상품선택) · 상세보기 모달 · 결제 · 예약완료(12자리 쿠폰 발급) · **메인 티켓 추천 섹션**.
> - **콘텐츠 소유(A안)**: 상세·안내·정책·위치·히어로·개요 = **뎁스(카테고리 S17)** / 상품(S14) = 판매옵션·연결스펙·판매기간·**썸네일**·날짜. (2.7 참조)
> - **이미지**: 어드민 전체(카테고리 콘텐츠·히어로·상품 썸네일·숙소/객실) **Cloudinary 업로드 → URL 저장**(자동 축소). base64 미저장. 실서비스=서버/CDN 업로드로 전환.
> - **언어**: 프런트 Nav·섹션 제목·카드 카테고리 뱃지 = **영어 고정**(숙소 정합, 외국인 전용). 히어로 카테고리명·검색결과 제목·버튼(담기 등) = 토글. **개발단계 KO-EN**(콘텐츠 `name_zh` 미노출) — **원 기획 의도는 EN-ZH**(추후 전환). (5.6 참조)
> - 어드민 셸: 상단 GNB + 컨텍스트 LNB. **티켓관리 LNB = 카테고리/컨텐츠 관리 · 쿠폰 스펙 관리 · 티켓 상품**(카테고리/컨텐츠 관리를 공통관리→티켓관리로 이동). 마진관리는 공통관리 LNB.
> - **이후 단계(미구현)**: S15/S15-B 주문 관리 · S16 발급현황 · 현장 POS 시뮬레이터 · S18-A 이메일 템플릿(v0.11). (주문·발급 쿠폰 데이터는 프런트 구매로 이미 생성됨 — 관리화면만 미구현)
> - 본 문서는 도메인 전체를 정의하되, 각 절에 **[구현]** / **[이후]** 로 단계를 표기한다.

---

## 0. 용어

| 용어 | 정의 |
|------|------|
| 입금가 | High1에 납부하는 원가. 고객 미노출. 마진 계산 기준. (쿠폰 스펙 적용금액) |
| 판매가 | 고객 청구 금액 = 입금가 + 마진. 소수점 버림. |
| 마진 | 판매가 − 입금가. 당사 수익. **카테고리 마스터 + 쿠폰ID 오버라이드**로 관리(우선순위 쿠폰ID > 2뎁스 > 1뎁스 > 전역). 한 쿠폰ID의 모든 레벨 동일. |
| 쿠폰 스펙 | High1 마케팅팀이 제공하는 쿠폰 정의(쿠폰ID·레벨·입금가·사용기간). S13에서 관리. |
| 쿠폰ID (coupon_id) | 쿠폰번호 앞 3자리 식별자. High1이 대행사별 부여(스펙 시트명). **티켓 관리·마진의 관리 축.** 예: DBO/DBR/DBT |
| 이용권명 | 스펙 파일 헤더의 쿠폰 식별명. 쿠폰ID와 1:1. 예: OAT_26성수기_얼리_워터월드(플레이스토리) |
| 레벨 (level) | 대인/소인 등 상품 구분자(**옵션**). 카테고리 뎁스 아님. 현장 성인/소인 별도 확인 없음(표시 전용). |
| 쿠폰번호 | 고객 발급 12자리. 우리 시스템이 **구매(결제 완료) 시점**에 생성. |
| 카테고리 (category) | 스키/워터파크 등 분류(최대 2뎁스). S17에서 직접 등록. S13 업로드 검증 기준·S14 드롭다운 소스. |
| 오픈 티켓(오픈형) | 날짜 미지정, 사용기간 내 자유 사용. **주 운영 방식.** |
| 커트오프 | 이용일 기준 예약 마감(N일/N시간 전 + 기준 시각). |

---

## 1. 시스템 아키텍처 (프로토타입 기준)

- **정적 SPA**: 빌드/프레임워크 없음. HTML + Vanilla JS + `localStorage`. 해시 라우팅. **숙소 어드민(`admin-prototype/`)에 티켓 도메인 통합**(동일 앱·저장소).
- **API 방향(숙소와 반대)**: 티켓 쿠폰 조회·사용·취소는 **High1(POS/KIOSK)이 우리(OTA) 서버를 호출**한다. 우리는 **API 서버**를 구현하고 URL을 제공한다. (숙소 PMS는 우리→High1 클라이언트)
- **실서비스 전환 시**: `localStorage` ↔ 서버 API/DB로 대체. 본 스펙의 엔티티·필드·규칙은 백엔드 스키마·API 계약으로 사용 가능.
- **이미지 저장(중요)**: 어드민 이미지(카테고리 콘텐츠·히어로·상세 인라인·상품 썸네일)는 **파일을 이미지 스토리지에 업로드하고 URL만 DB에 저장**한다. base64 인라인 저장 금지(용량·성능). **프로토타입은 Cloudinary 무인증 업로드**(업로드 전 1600px·JPEG 축소, `res.cloudinary.com/...` URL 저장), **실서비스는 서버 서명 업로드 + 자사/CDN 스토리지**로 구현. → 모델의 `*_meta[].data_url`·리치 HTML `<img src>`에는 **URL**이 들어간다.
- **다국어**: 외국인 전용. **원 기획 의도 = EN·ZH**(EN 필수 · ZH 선택). 카테고리 **관리명(KO)** 은 내부 식별·CSV 매칭용 별도 보유(고객 미노출). ※ **개발단계는 KO-EN 토글**로 진행 중이며 콘텐츠 ZH(`name_zh`)는 프런트 미노출(추후 EN-ZH 전환 시 복원). 구조적 UI(Nav·섹션 제목·카드 뱃지)는 **영어 고정**(5.6).
- **어드민 셸**: **상단 GNB + 컨텍스트 LNB** 2단. GNB=숙소관리·티켓관리·투어관리(MOCK)·예약관리·화면관리·공통관리. **티켓관리 LNB = 카테고리/컨텐츠 관리 · 쿠폰 스펙 관리 · 티켓 상품**(카테고리/컨텐츠 관리를 공통관리→티켓관리 최상단으로 이동, v0.7.10). 마진관리는 공통관리 LNB.
- **어드민 라우트**: `#/ticket-categories[/edit/:id]`(S17 목록·콘텐츠 편집) · `#/coupon-specs[/history]`(S13) · `#/ticket-products[/new|/edit/:id]`(S14-A/B) · `#/margin-management`(S11 숙소/티켓 탭) · `#/main-management/section/new-ticket|edit/:id`(티켓 추천 섹션).
- **프런트 라우트**: `#/ticket/search` · `#/ticket/bridge/:catKo` · `#/ticket/checkout` · `#/ticket/done`. 검색바 도메인 토글(Accommodation/Ticket)·GNB Ticket 드롭다운(카테고리).

---

## 2. 데이터 모델 (localStorage 엔티티)

> 텍스트 표시 필드는 `*_en`(필수)/`*_zh`(선택). 카테고리는 추가로 `name_ko`(관리명). `id`는 시스템 고유(uid). 날짜 `YYYY-MM-DD`.

### 2.1 저장소 키 일람

| 키 | 엔티티 | 쓰기 | 읽기 | 단계 |
|----|--------|------|------|------|
| `high1_ticket_categories_v1` | 티켓 카테고리[] (2뎁스) + **뎁스 콘텐츠** | S17 | S13 검증·S14·프런트 브릿지 | 구현 |
| `high1_coupon_specs_v1` | 쿠폰 스펙[] (쿠폰ID+레벨 행) | S13 | S14·S16·프런트 | 구현 |
| `high1_ticket_products_v1` | 티켓 상품[] (**썸네일만**) | S14 | 프런트·S15 | 구현 |
| `high1_ticket_margin_master_v1` | **{categories, overrides}** 마진 | S11 티켓 탭 | S14-B(표시)·판매가 | 구현 |
| `high1_ticket_upload_history_v1` | S13 업로드 이력[] | S13 | S13 탭B | 구현 |
| `high1_ticket_seed_ver` | 더미 시드 버전 마커 | ensureTicketStores | — | 구현(프로토타입 전용) |
| `high1_ticket_orders_v1` | 티켓 주문[] | 프런트 구매·S15 | S15-B·S16 | 데이터=구현 / 관리화면=이후 |
| `high1_coupons_v1` | 발급 쿠폰[] (12자리) | 프런트 구매·S15 | S16·현장 API | 데이터=구현 / 관리화면=이후 |

> **프로토타입 더미 시드**: `high1_ticket_seed_ver` 값이 코드 상수 `TICKET_SEED_VER`와 다르면 카테고리·스펙·마진·상품을 재주입(업로드 이력 제외). 더미 갱신 시 상수를 올린다. 실서비스에는 해당 마커 없음.
> **현재 더미(seed_ver=12)**: 워터파크 5코드(DBO/DBR/DBT 종일권·DBQ 4시간·DBW 7시간) · 스키 리프트권 3(SVC/SVZ/SVA) · 스키 장비렌탈 3(RSA/RSC/RSF) 각 대인/소인 + **곤돌라 GDO(성인 전용, 대인만)** = 12코드·23행. 워터월드 종일권 외 입금가는 추정.

### 2.2 TicketCategory (카테고리 + 뎁스 콘텐츠) — `high1_ticket_categories_v1` [구현]

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | string | 고유 id |
| `code` | string | `TC-###`(자동채번) |
| `name_ko` | string | **관리명(KO)** — 내부 식별·CSV 매칭키. 필수 |
| `name_en` | string | 표시명 EN **필수** |
| `name_zh` | string | 표시명 ZH(선택) |
| `parent_id` | string\|null | 1뎁스=null / 2뎁스=상위 id |
| `order` | number | 노출 순서 |
| `hero_image_meta` | `[{data_url,name}]` | **히어로 이미지**(프런트 브릿지 상단) |
| `overview_en`/`overview_zh` | html | **개요**(리치) |
| `detail_html_en`/`detail_html_zh` | html | **상세설명**(리치·이미지 포함) |
| `guide_html_en`/`guide_html_zh` | html | **이용안내**(리치) |
| `policy_html_en`/`policy_html_zh` | html | **유의사항/정책**(리치) |
| `address_en`/`address_zh` | string | **위치(주소)** |
| `updatedAt` | ISO | 메타 |

> 최대 2뎁스. **삭제 없음**(추가·수정만). 레벨(대인/소인)은 카테고리 아님(상품 옵션).
> **뎁스 콘텐츠(A안)**: 상세·안내·정책·위치·히어로·개요는 **카테고리(말단 뎁스) 소유** — 2뎁스가 있으면 2뎁스가, 없으면 1뎁스가 콘텐츠를 가진다. S17 "카테고리/컨텐츠 관리"에서 **리치 에디터**([이미지 불러오기]·붙여넣기·굵게·제목·목록)로 편집. 프런트 브릿지 섹션에 매핑.

### 2.3 CouponSpec (쿠폰 스펙) — `high1_coupon_specs_v1` [Phase1]

> 행 1개 = (쿠폰ID + 레벨) 1건. 같은 `coupon_id`의 여러 레벨 행이 하나의 스펙을 구성.

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | string | 고유 id |
| `coupon_id` | string | **쿠폰ID(3자리)** — 스펙 시트명. 관리·마진 축 |
| `pass_name` | string | 이용권명(스펙 헤더 식별명) |
| `category` | string | 카테고리 관리명(name_ko) |
| `product_type` | string | 2뎁스 관리명(없으면 "") |
| `level` | number(1~8) | 레벨 번호 |
| `level_name` | string | 레벨명(대인/소인 등) |
| `coupon_name` | string | 레벨별 고객 표시명 |
| `price` | number | **입금가**(원가) |
| `discount_target` | string | 할인대상 내역(PMS 상품 목록) |
| `use_start_date`/`use_end_date` | date | 사용기간(표시용 — 날짜 유효성 체크 안 함) |
| `active` | boolean | 노출 운영 토글(쿠폰ID 단위). false=연결 상품 강제 미노출(🔒) |
| `created_at`/`updated_at` | ISO | 메타 |

### 2.4 TicketProduct (티켓 상품) — `high1_ticket_products_v1` [Phase1]

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | string | 고유 id |
| `product_code` | string | `PR-T####`(자동채번) |
| `name_en`/`name_zh` | string | 상품명 EN 필수/ZH |
| `category_1` | string | 1뎁스 카테고리 관리명 |
| `category_2` | string | 2뎁스 관리명(없으면 "") |
| `spec_coupon_id` | string | **연결 쿠폰ID**(필수, 스펙 1:상품 N). S14-B에서 **카테고리1(+2) 선택 → 해당 카테고리 스펙으로 필터**(1개면 자동선택) |
| `visibility` | `Y`\|`N` | 노출 여부 |
| `sale_start_date`/`sale_end_date` | date\|"" | **판매기간**(우리 홈페이지 판매 가능 기간, v0.9). 시작 미설정=즉시 / 종료 미설정=토글 제어 / 종료 경과=자동 미노출. 사용기간(현장)과 별개 |
| `date_mode` | `open` | 날짜 방식(오픈형 고정. 날짜지정형=이후) |
| `cutoff` | `{n, unit:"day"\|"hour", time:"HH:MM"}` | 커트오프. 미설정 시 당일 자정까지 |
| `cancel_policy[]` | `{base, penalty}` | (날짜지정형 대비 보존) 이용일 기준 구간. **오픈형은 미사용**(2.4.1 특례) |
| `thumb_meta` | `[{data_url,name}]` | **썸네일**(검색결과·브릿지 카드·메인 추천 대표 이미지). 첫 장이 대표 |
| `created_at`/`updated_at` | ISO | 메타 |

> **상품은 썸네일만 소유** — 히어로·상세소개·이용안내·정책·위치는 뎁스(S17)가 가진다(A안, 2.7). 상세보기 모달은 옵션가·사용기간 표만 표시(이미지 갤러리·상품 상세 없음).

#### 2.4.1 취소정책 — 오픈형 특례 (v0.11 8.2, [Phase1])

**오픈형 상품은 이용일이 없으므로 이용일 기준 구간(`cancel_policy`)을 사용하지 않고, 사용기간 종료일 기준 고정 규칙을 적용한다.** S14-B에서 편집 UI 없이 읽기전용 안내로 표시.

| 취소 시점 | 쿠폰 상태 | 환불 |
|---|---|---|
| 사용종료일(`use_end_date`) 이전 | 미사용(SOLD) | 전액 환불 |
| 사용종료일 이후 | 미사용(SOLD) | 환불 불가 |
| 사용 완료 | USED | 환불 불가 (사용취소는 High1 /cancel만) |

> `use_end_date`는 연결 쿠폰 스펙(S13)에서 가져온다. `cancel_policy[]`(base `3plus`/`2`/`1`/`today` + penalty%)는 날짜지정형 전환 대비 스키마만 유지.

### 2.5 마진 마스터 — `high1_ticket_margin_master_v1` [구현]

```
{
  categories: { "워터파크": {type,value}, "스키>리프트권": {type,value}, ... },  // 카테고리 마스터 (키 = 1뎁스 또는 "1뎁스>2뎁스")
  overrides:  { "DBQ": {type,value}, ... }                                       // 쿠폰ID 개별 오버라이드
}
```
- **적용 우선순위(정책 v0.11 §11.3)**: ① `overrides[coupon_id]` > ② `categories["1뎁스>2뎁스"]` > ③ `categories["1뎁스"]` > ④ **전역 디폴트** `DEFAULT_TICKET_MARGIN = {type:"amount", value:"30000"}`(마진금액 30,000원).
- 한 쿠폰ID의 모든 레벨 동일 마진. 쿠폰ID의 카테고리는 스펙(category/product_type)에서 조회.
- 편집 화면 = **S11 마진관리 > 티켓 탭 [구현]**: 카테고리 마스터 설정 바([카테고리 마스터 적용]=상속 행에 반영·임시) + 쿠폰ID 목록(상속/개별 토글·레벨별 판매가 예시) + [쿠폰ID 마진 저장](localStorage 확정·저장 확인/완료 알럿).
- **구 flat 스키마**(`{coupon_id:{...}}`)는 로드 시 `overrides`로 자동 마이그레이션.

### 2.6 업로드 이력 — `high1_ticket_upload_history_v1` [Phase1]

```
[{ id, at(ISO), filename, total, success, fail, uploader, rows:[{line, coupon_id, level, status:"success"|"overwrite"|"fail", reason}] }]
```
- 최근 50건 보관. S13 탭B에서 인라인 상세로 실패 라인·사유 조회.

### 2.7 콘텐츠 소유 (A안) [구현]

| 콘텐츠 | 소유 | 입력(어드민) | 노출(프런트) |
|--------|------|------|------|
| 히어로 이미지 | **뎁스(카테고리)** | S17 | 브릿지 상단 히어로 |
| 개요 | **뎁스** | S17(리치) | 브릿지 개요 섹션 |
| 상세설명 | **뎁스** | S17(리치) | 브릿지 상세설명 섹션 |
| 이용안내 | **뎁스** | S17(리치) | 브릿지 안내 섹션 |
| 유의사항/정책 | **뎁스** | S17(리치) | 브릿지 정책 섹션 |
| 위치(주소) | **뎁스** | S17 | 브릿지 위치 섹션 |
| **썸네일** | **상품** | S14 탭1 | 검색결과·브릿지 카드·메인 추천 |
| 판매옵션·연결스펙·판매기간·날짜·커트오프 | **상품** | S14 | — |

> 같은 뎁스 내 상품들은 시설 소개를 공유하므로 콘텐츠를 뎁스에서 한 번만 관리한다. 상품에는 목록 대표 썸네일만 둔다. (상품별 상세소개·히어로 필드 폐지)

### 2.8 주문·발급 쿠폰 (프런트 구매 시 생성) [데이터=구현 / 관리화면=이후]

- `high1_ticket_orders_v1`: `{ id(HM-…/주문번호), buyer{last,first,email,phone,nationality}, items:[{product_id, level, qty}], total, created_at }`. 다상품 한 주문(장바구니).
- `high1_coupons_v1`: `{ no(12자리), coupon_id, level, order_id, product_id, status:"SOLD", sell_price, used_at:null, created_at }`. 구매(결제 완료) 시 수량만큼 발급(SOLD).

---

## 3. 공통 규칙

### 3.1 노출(고객 화면) 조건 — OR 미노출 (v0.9)
```
프런트 노출 = (spec.active !== false) AND (product.visibility !== "N") AND (판매기간 내)
판매기간 내 = (sale_start 미설정 OR today >= sale_start) AND (sale_end 미설정 OR today <= sale_end)
```
- **미노출 조건 중 하나라도 해당되면 미노출**: ① 스펙 토글 OFF(Cascade 🔒) ② 상품 노출 토글 OFF ③ 판매종료일 자정 경과(자동).
- **Cascade**: 쿠폰 스펙 `active=false`(S13 노출 OFF) → 연결 티켓 상품 전체 강제 미노출(어드민에 🔒).

### 3.2 마진·판매가 계산 (`computeSellPrice(base, type, value)` — 숙소와 공용)
```
amount: 판매가 = base + floor(value)          // value 음수 허용(할인)
rate  : 판매가 = floor(base × (1 + value/100)) // value 정수 -49 ~ +100
그 외 : 판매가 = base
※ 결과 min 0, 소수점 무조건 버림(floor)
```
- 티켓 마진 = **카테고리 마스터 + 쿠폰ID 오버라이드로 해석**(2.5 우선순위), 날짜별 차등 없음(오픈형). 한 쿠폰ID의 모든 레벨 동일. 판매가 = 입금가 + 적용 마진.

### 3.3 식별자
- 쿠폰ID(3자리, High1 부여) / 쿠폰번호(12자리, 2.7) / 카테고리 `TC-###` / 상품 `PR-T####`. TC·PR은 자동채번.

### 3.4 쿠폰번호 규칙 (12자리) [이후 — 발급 단계]
```
[쿠폰ID 3][레벨 1][일련번호 6][난수 2]   예: DBO100000157
```
- 일련번호 000001~999999 순차 발급. 난수 2자리=패턴 노출 방지.
- **구매(결제 완료) 시점 생성** → 발급 쿠폰은 처음부터 SOLD. AVAILABLE 상태 행 없음.
- 유니크: 동일 쿠폰ID+레벨 내. CANCELED 번호 재사용 가능, USED 불가.

### 3.5 쿠폰 상태머신 [이후 — 현장 API 단계]
```
SOLD ──/use──▶ USED ──/cancel──▶ SOLD(복원)
  └──주문취소──▶ CANCELED
```
- 재고 개념 없음(재고=발급 쿠폰 수). 1쿠폰=1영업장(복합구매 시 권종별 별도 발급). VOID 미운영.

---

## 4. 어드민 화면 스펙

> 등록 필수 순서: **카테고리(S17) → 쿠폰 스펙(S13) → 티켓 상품(S14)**.

### S17 카테고리/컨텐츠 관리 (`ticket-categories`) [구현]
- **티켓관리 LNB 최상단**. 최대 2뎁스 트리. **추가·수정만(삭제 없음)**.
- 등록 모달: `상위 카테고리`(readonly) · **관리명 KO(필수)** · **명칭 EN(필수)** · 명칭 ZH(선택) · 노출 순서. 같은 뎁스 내 관리명 중복 금지.
- **콘텐츠 편집**(`ticket-categories/edit/:id`): 말단 뎁스별 ① 히어로 이미지 ② 개요 ③ 상세설명 ④ 이용안내 ⑤ 유의사항/정책 ⑥ 위치(주소). ②~⑤는 **리치 에디터**(EN/ZH). → 프런트 브릿지 섹션에 매핑(2.7).

### S13 쿠폰 스펙 관리 (`coupon-specs`) [Phase1]
- 티켓관리 LNB. **탭A(업로드 & 스펙 목록) / 탭B(업로드 이력)**.
- **업로드**: (실서비스=CSV 파일 업로드) / **프로토타입=[샘플 스펙 불러오기] 버튼으로 더미 CSV 주입**. CSV 템플릿 다운로드 제공.
  - **CSV 컬럼(정책 v0.7)**: 카테고리*·상품유형·쿠폰코드*·이용권명*·레벨*·쿠폰명*·입금가*·할인대상내역·사용시작일*·사용종료일*
  - **선행 검증**: `카테고리`/`상품유형` 값이 S17 관리명(KO)과 일치해야 함. 불일치 행 = 실패("카테고리 불일치").
  - **재업로드**: 동일 쿠폰ID+레벨 존재 시 덮어쓰기 확인(취소 시 전체 중단). 덮어쓰기 시 기존 `active` 보존.
  - 결과: 전체/성공/실패 요약 + 탭B 이력 기록(실패 라인·사유 상세).
- **스펙 목록**: 쿠폰ID 그룹핑(그룹 헤더 + 레벨 행). 컬럼 = 레벨/코드·쿠폰명·입금가·사용기간·연결상품·발급쿠폰·**노출 ON/OFF**·삭제.
- **노출 토글**: 쿠폰ID 단위. OFF → 연결 상품 강제 미노출(🔒 Cascade).
- **삭제**: 연결 상품 0 AND 발급 쿠폰 0 일 때만(그 외 비활성 + 툴팁).

### S14-A 티켓 상품 목록 (`ticket-products`) [Phase1]
- 쿠폰ID 마스터 행 + 레벨 디테일 행(입금가·마진·판매가). 컬럼 = 쿠폰ID·상품명·카테고리·날짜방식·레벨/마진·판매가·노출·수정/삭제.
- 노출 토글: 상품 `visibility`. 연결 스펙 OFF면 강제 미노출(🔒, 토글 잠금).
- `+ 상품 등록`(스펙 0건이면 알럿).

### S14-B 티켓 상품 마스터 (`ticket-products/new|edit/:id`) [구현 · 2탭]
- **탭1 상품 마스터**:
  - ① 기본정보: 상품명 EN(필수)/ZH · **카테고리 1뎁스(필수)+2뎁스 → 연결 스펙(쿠폰ID) 자동 필터**(1개면 자동선택) · 노출 여부 · **판매기간(선택)**.
  - ② 레벨 구성 & 판매가(**마진 읽기전용**): 연결 스펙 레벨별 입금가 · 적용 마진(2.5 해석) · 판매가(computeSellPrice). 편집 링크 → S11 마진관리 티켓 탭.
  - ③ 취소/환불 정책: **오픈형 고정 안내(읽기전용)** — 사용기간 종료일 기준(2.4.1).
  - ③ **썸네일**: 목록 대표 이미지(검색결과·브릿지 카드·메인 추천). 상세·안내·정책·히어로는 S17(뎁스)에서 관리한다는 안내.
- **탭2 날짜 설정**: 날짜방식=**오픈형 고정**. **커트오프는 오픈형에서 숨김**(안내문구만).
- ※ 종전 탭3(이미지 히어로·서브)·탭4(상세정보)는 **폐지** — 콘텐츠 뎁스화(A안)로 상품엔 썸네일만.
- 저장 유효성: 상품명 EN · 카테고리 1뎁스 · 연결 스펙 · 판매종료일 ≥ 시작일.

### S11 마진관리 — 숙소/티켓 탭 (`margin-management`) [구현]
- 공통관리 LNB. **숙소 탭**(객실 타입 RM_TYP_CD별) / **티켓 탭**.
- **티켓 탭**(2.5): 우선순위 안내 + **카테고리 마스터 설정 바**(카테고리 전체/1뎁스/2뎁스 + 유형·값 + [카테고리 마스터 적용]→상속 행 반영) + **쿠폰ID 목록**(카테고리·이용권명·상속/개별 배지·유형·값·레벨별 판매가 예시·[개별설정]/[상속으로] 토글) + **[쿠폰ID 마진 저장]**(확정·저장 확인/완료 알럿). 저장 `{categories, overrides}`.

### S15 / S15-B 티켓 주문·주문상세 [이후 — 관리화면 미구현]
- 주문 목록(주문상태 결제완료/취소완료 · 사용상태 미사용/일부사용/사용완료) → 상세(발급 쿠폰 7컬럼·전체취소·이력). **전체취소만**(부분취소·부분환불 없음). ※ 주문·발급 쿠폰 데이터는 프런트 구매로 이미 생성(2.8).

### S16 쿠폰 발급현황 [이후]
- 쿠폰 1장=1행. 상태 SOLD/USED/CANCELED(AVAILABLE·VOID 없음). 통계·엑셀·현장 API 연동정보 표시.

---

## 5. 프런트 화면 스펙 [구현]

> 티켓 프런트는 **숙소 프런트와 동일한 컴포넌트·CSS를 재사용**해 이질감을 없앤다(검색결과=`.product-card`, 브릿지=`.place-selector`/`.hero-fullwidth`/`.place-nav`, 추천=`.rec-card`).

### 5.1 진입 · 검색바
- **검색바 도메인 토글**: Accommodation / Ticket. Ticket 선택 시 **자동 이동하지 않고** 바만 티켓 모드로 전환(사용일 단일 선택, **기본값 오늘+7일**), [검색] 클릭 시 `#/ticket/search` 이동. 메인 진입 시 도메인 숙소 기본.
- **GNB Ticket** 드롭다운: 카테고리 목록(전체 티켓 없음). Ticket 글자 클릭 → 첫 카테고리 브릿지.

### 5.2 검색결과 (`#/ticket/search`) — 숙소식
- 좌측 필터(정렬 낮은/높은 가격순 · 카테고리 체크박스 · 초기화) + 우측 canonical 카드(`.product-card`).
- 카드 = 썸네일 · 카테고리 뱃지 · 상품명 · **🗓 사용기간 · 👥 인원(성인·아동/성인 예약가능)** · 옵션가 · 최저가 · [상세보기].
- 필터: 노출조건(3.1) AND 사용일(선택 시 그날 사용기간 내). 카드 클릭 → 카테고리 브릿지 / [상세보기] → 모달.

### 5.3 카테고리 브릿지 (`#/ticket/bridge/:catKo`) — 숙소식 장바구니형
- 2뎁스 서브탭(place-selector, 히어로 위) · 히어로(hero-fullwidth, 로고 정렬) · Sticky Nav(**영어 고정** Overview/Select/Detail/Guide/Policy/Location — 5.6).
- 콘텐츠 섹션 = **뎁스(S17) 로드**. **상품선택** = 좌 상품(성인/아동 스테퍼 + 실시간 소계 + [상세보기]/[담기]) / 우 sticky 장바구니(라인 ✕ 삭제, [예약하기]).
- **다상품 한 주문**: 여러 상품·레벨을 한 장바구니에 담아 예약(장바구니 범위=1뎁스 브릿지). 담기 버튼명 "총 N매 , NN원 담기".
- 상세보기 모달 = 사용기간·옵션가 표만(이미지·상품상세 없음).

### 5.4 결제·완료 (`#/ticket/checkout` → `#/ticket/done`)
- 결제: 주문 상품 요약(다상품) + 구매자 정보 → 결제(목업) → **예약완료**(주문번호 + 12자리 발급 쿠폰). 주문·쿠폰 생성(2.8).

### 5.5 메인 추천 섹션 [구현]
- 어드민 화면관리 > 메인관리 추천 섹션에 **유형(숙소/티켓)** 도입. 티켓 섹션(`type:"ticket"`) = 자동(카테고리 필터+정렬 최저가/이름) 또는 수동(상품 직접 선택).
- 프런트 메인에 티켓 카드(`.rec-card` 동일 규격: 썸네일·카테고리·상품명·인원·최저가·[예약하기]) 가로 스트립 → 클릭 시 카테고리 브릿지.

### 5.6 언어 동작 (숙소 정합) [구현]
- 외국인 전용 사이트 원칙에 따라 **구조적 UI는 영어 고정**, **콘텐츠 명칭만 토글**한다(숙소 프런트와 동일 규칙).
  - **영어 고정**(토글 안 함): 브릿지 Sticky Nav(Overview/Select/Detail/Guide/Policy/Location), 섹션 제목, 검색·추천 카드의 **카테고리 뱃지**.
  - **토글**(KO/EN): 히어로 카테고리명, 검색결과 페이지 제목, 버튼·본문 라벨(담기 등).
- **개발단계 KO-EN**: 언어 토글은 KO/EN. 콘텐츠 `name_zh`(중국어)는 프런트 미노출(코드에 복원 주석). **원 기획 의도는 EN-ZH** — 추후 토글을 EN/ZH로 교체하고 `name_zh` 노출로 전환.

---

## 6. 비즈니스 로직 (백엔드 계약)

### 6.1 판매가 노출 [Phase1]
- 노출 상품(3.1) × 레벨별 판매가 = 입금가 + 쿠폰ID 마진(3.2). 입금가·마진은 고객 미노출.

### 6.2 노출 Cascade [Phase1]
- 쿠폰 스펙 `active=false` → 연결 상품 전체 미노출. 상품 `visibility=N` → 개별 미노출.

### 6.3 쿠폰 발급 [이후]
- 구매(결제 완료) 시 12자리 쿠폰번호 생성(3.4), 상태 SOLD. 주문(S15)·발급이력(S16) 생성.

### 6.4 현장 사용/취소 상태머신 [이후]
- High1 POS/KIOSK가 우리 API 호출 → SOLD↔USED↔(주문취소)CANCELED (3.5, 7절).

### 6.5 취소/환불 [이후]
- 주문 단위 **전체취소만**. 위약금율(취소정책) × 판매가. 단품=High1 통보 불필요. 패키지=API 통보(미개발 M-03).

---

## 7. 현장 연동 API (우리가 구현할 서버) [이후 — POS 시뮬레이터 단계]

> 인증 없음 · HTTP GET · **날짜 유효성 체크 미구현(DBER31/32 미발생)**. 응답 = `BARCODE + RESULT + MSG`.

| API | 규격서 엔드포인트 | 성공(DBOK) | 실패 |
|-----|------------------|------------|------|
| 조회 | `hiww_ent_search.jsp?BARCODE=` | USE ABLE(SOLD) | DBER01 자릿수 · DBER02 미존재 · DBER03 이미사용 · DBER04 취소됨 |
| 사용 | `hiww_ent_use.jsp?BARCODE=` | USE OK(SOLD→USED) | DBER02 · DBER03 · DBER04 |
| 사용취소 | `hiww_ent_usecancel.jsp?BARCODE=` | USECANCEL OK(USED→SOLD) | — |

- 각 API 독립 동작(조회 없이 사용 단독 호출 유효). URL은 우리가 High1 담당자에게 전달 → POS/KIOSK 설정 반영.

---

## 8. 예외·엣지 케이스

| # | 상황 | 처리 | 단계 |
|---|------|------|------|
| 1 | S13 CSV 카테고리 S17 미등록 | 해당 행 업로드 실패("카테고리/상품유형 불일치") | Phase1 |
| 2 | S13 재업로드(동일 쿠폰ID+레벨) | 덮어쓰기 확인(취소 시 전체 중단), 기존 active 보존 | Phase1 |
| 3 | EN 필수 필드 미입력(카테고리·상품) | 저장 차단 | Phase1 |
| 4 | 쿠폰 스펙 노출 OFF | 연결 상품 전체 강제 미노출(🔒) | Phase1 |
| 5 | 스펙 0건에서 상품 등록 | 알럿 후 차단 | Phase1 |
| 6 | 스펙 삭제(연결 상품/발급 쿠폰 존재) | 삭제 버튼 비활성 + 툴팁 | Phase1 |
| 7 | 취소정책 0구간 / 기준일 중복 | 저장 차단 | Phase1 |
| 8 | 연결 스펙 삭제/부재 | 상품 목록에 "연결 스펙 없음" 경고 | Phase1 |
| 9 | ZH 미입력 | EN으로 대체 표시 | Phase1 |
| 10 | 구매 취소 | 주문 전체취소(SOLD→CANCELED), 위약금율 적용 | 이후 |
| 11 | USED 쿠폰 포함 주문 취소 | 운영정책 미확정(신규-3) | 이후 |

---

## 9. 미결 / 후속 (개발 착수 전 확정)

| 항목 | 현황 | 필요 조치 |
|------|------|-----------|
| **우리 전용 쿠폰ID(M-04)** | 미수령 | 프로토타입은 샘플 코드(DBO/DBR/DBT) 차용. High1 발급 후 교체 |
| **스키 입금가(신규-1 관련)** | 리프트 스펙 미수령·프로토타입 추정값(리프트 SVC/SVZ/SVA·장비 RSA/RSC/RSF) | 실 스펙 수령 후 교체 |
| HTTPS/타임아웃(M-01·M-02) | 미확인 | High1 개발팀 확인 |
| 패키지 매핑·취소통보 API(M-03) | 미개발 | 단품 연동 완료 후 협의(패키지=본 스펙 제외) |
| USED 포함 주문 전체취소(신규-3) | 미확정 | 운영정책 확정 |
| 티켓 마진 저장 키(신규-4) | `high1_ticket_margin_master_v1` `{categories,overrides}` 스키마 확정·구현 | 확정 |
| **이미지 스토리지** | 프로토타입=Cloudinary 무인증 업로드(URL 저장). 무인증 preset 클라이언트 노출 | 실서비스=서버 서명 업로드 + 자사/CDN 스토리지로 구현(개발사) |
| 판매유형(얼리/상시/프로모션) 관리(M-12) | 제외 | 필요 시 재검토 |

### 화면기획서 정합 플래그 (대화창 2)
- 쿠폰코드는 실제 **DBO/DBR/DBT**(시트명), "OAT"는 이용권명 접두어 → 화면기획서 `OAT100...` 예시 재검토
- **S14-C** 기준 버전 v0.5(타 화면 v0.7~v0.8), "워터파크 > 대인" 카테고리 예시 오류(대인=레벨), 쿠폰번호 예시 포맷 화면 간 불일치
- 카테고리 **관리명(KO)**: 프로토타입은 CSV 매칭·내부 식별용 KO를 추가(화면기획서는 EN/ZH만) — 정합 검토 필요

---

*본 스펙 v1.3은 어드민 프로토타입 v0.7.18 / 프런트 v0.8.9 / 정책서 v0.11 기준. 패키지·투어·날짜지정형·S15/S16 주문·발급 관리화면·POS 시뮬레이터·S18-A 이메일은 별도 라인/이후 단계. 변경 시 버전 갱신. 자매 문서: `High1-OTA-통합개발스펙-숙소-v1.2.md`*
